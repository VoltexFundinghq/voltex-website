import ComingSoon from "@/components/admin/ComingSoon";
export default function Page() { return <ComingSoon title="Email Queue" note="We don't currently persist a send queue — Resend sends are fire-and-forget. Worth deciding if this is worth building." />; }
